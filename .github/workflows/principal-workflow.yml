name: Principal Workflow

on:
  push:
    branches: [main]

jobs:
  scan:
    uses: ./shared-workflows/.github/workflows/security-scan.yml

  build:
    needs: scan
    uses: ./shared-workflows/.github/workflows/go-build.yml
